language: ruby
cache: bundler
services: redis

jdk:
  - oraclejdk7

rvm:
  - 2.0.0
  - ruby-head
  - jruby-head
  - rbx-head

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
    - rvm: rbx-head
